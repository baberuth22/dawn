{% schema %}

{
  "name": "➕ SA - Form Builder",
  "settings": [
    {
      "type": "text",
      "id": "form_title",
      "label": "Form Title",
      "default": "Custom Form"
    },
    {
      "type": "number",
      "id": "form_width",
      "label": "Form Width (px)",
      "default": 600
    },
    {
      "type": "number",
      "id": "form_height",
      "label": "Form Height (px)",
      "default": 400
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button Color",
      "default": "#007bff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button Text Color",
      "default": "#ffffff"
    },
    {
      "type": "font_picker",
      "id": "font_family",
      "label": "Font",
      "default": "sans-serif"
    },
    {
      "type": "range",
      "id": "text_font_size",
      "label": "Text Font Size (px)",
      "default": 16,
      "min": 10,
      "max": 30
    },
    {
      "type": "range",
      "id": "button_font_size",
      "label": "Button Font Size (px)",
      "default": 16,
      "min": 10,
      "max": 30
    }
  ],
  "blocks": [
    {
      "type": "radio_question",
      "name": "Radio Question",
      "settings": [
        {
          "type": "text",
          "id": "question_label",
          "label": "Question Label",
          "default": "Sample Question?"
        },
        {
          "type": "text",
          "id": "option_a",
          "label": "Option A",
          "default": "Yes"
        },
        {
          "type": "text",
          "id": "option_b",
          "label": "Option B",
          "default": "No"
        }
      ]
    },
    {
      "type": "text_question",
      "name": "Text Question",
      "settings": [
        {
          "type": "text",
          "id": "question_label",
          "label": "Question Label",
          "default": "What is your name?"
        },
        {
          "type": "checkbox",
          "id": "required",
          "label": "Required",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "➕ SA - Form Builder",
      "category": "Forms"
    }
  ]
}

  
{% endschema %}

{%  style %}
/* PAGE & INPUT STYLING */
body {
  background: #000;
  color: #FFF;
}

.btn {
  border: 1px solid #808080;
  display: inline-block;
  padding: 5px 10px;
  font-size: 20px;
  position: relative;
  text-align: left;
  border-radius: 3px;
  -webkit-transition: background 600ms ease, color 600ms ease;
  transition: background 600ms ease, color 600ms ease;
}
.btn span {
  border: 1px solid #808080;
  display: inline-block;
  padding: 1px 6px;
  font-size: 12px;
  border-radius: 5px;
  vertical-align: middle;
  text-align: center;
  margin-top: -5px;
}

input[type="radio"].toggle {
  display: none;
}
input[type="radio"].toggle + label {
  cursor: pointer;
  min-width: 80px;
}
input[type="radio"].toggle + label:hover {
  background: none;
  color: #FFF;
}
input[type="radio"].toggle + label:after {
  content: "";
  height: 100%;
  position: absolute;
  top: 0;
  -webkit-transition: left 100ms cubic-bezier(0.77, 0, 0.175, 1);
  transition: left 100ms cubic-bezier(0.77, 0, 0.175, 1);
  width: 100%;
  z-index: -1;
}
input[type="radio"].toggle.toggle-left + label {
}
input[type="radio"].toggle.toggle-left + label:after {
  left: 100%;
}
input[type="radio"].toggle.toggle-right + label {
  margin-left: 10px;
}
input[type="radio"].toggle.toggle-right + label:after {
  left: -100%;
}
input[type="radio"].toggle:checked + label {
  background: #FFF;
  cursor: default;
  color: #000;
}
input[type="radio"].toggle:checked + label:after {
  left: 0;
}

/* ENDS */

/* UPFORM STYLE STARTS*/
.upform input:focus, select:focus, textarea:focus, button:focus {
  outline: none;
  border-color: blue !important;
}
.upform input, select, textarea {
  background-color: #000 !important;
  color: #FFF;
}
.upform {
  font-family: 'Open Sans', sans-serif;
  -webkit-touch-callout: none; /* iOS Safari */
  -webkit-user-select: none; /* Safari */
  -khtml-user-select: none; /* Konqueror HTML */
  -moz-user-select: none; /* Firefox */
  -ms-user-select: none; /* Internet Explorer/Edge */
  user-select: none; /* Non-prefixed version, currently
		            supported by Chrome and Opera */
  max-width: 900px;
  margin: 300px auto;
  margin-bottom: 500px;
  padding: 0 20px;
}

.upform .upform-main {
}
.upform .upform-main .input-block {
  padding: 30px 0;
  opacity: 0.25;
  cursor: default;
}
.upform .upform-main .input-block .label {
  display: block;
  font-size: 1.1em;
  line-height: 30px;
}
.upform .upform-main .input-block .input-control {
  margin: 20px 0;
}
.upform .upform-main .input-block .input-control input[type=text] {
  border: none;
  outline-width: 0;
  border-bottom: 2px solid #CCC;
  width: 100%;
  font-size: 35px;
  padding-bottom: 10px;
}

.upform .upform-main .input-block.active {
  opacity: 1;
}

.upform .upform-footer {
  margin-top: 60px;
}
.upform .upform-footer .btn {
  font-size: 24px;
  font-weight: bold;
  padding: 5px 20px;
}
/* UPFORM STYLE ENDS*/

{% endstyle %}

<div class="upform" style="width: {{ section.settings.form_width }}px; height: {{ section.settings.form_height }}px; background-color: {{ section.settings.background_color }};">
  <form>
    <div class="upform-header">
      <h2 style="font-family: {{ section.settings.font_family }}; font-size: {{ section.settings.text_font_size }}px; color: {{ section.settings.text_color }};">{{ section.settings.form_title }}</h2>
    </div>

    <div class="upform-main">
      {% for block in section.blocks %}
        {% if block.type == 'radio_question' %}
          <div class="input-block">
            <div class="label" style="font-family: {{ section.settings.font_family }}; font-size: {{ section.settings.text_font_size }}px; color: {{ section.settings.text_color }};">{{ block.settings.question_label }}</div>
            <div class="input-control">
              <input id="toggle-on-q{{ forloop.index }}" class="toggle toggle-left" name="q{{ forloop.index }}" value="{{ block.settings.option_a | escape }}" type="radio">
              <label for="toggle-on-q{{ forloop.index }}" class="btn"><span>A</span> {{ block.settings.option_a }}</label>
              <input id="toggle-off-q{{ forloop.index }}" class="toggle toggle-right" name="q{{ forloop.index }}" value="{{ block.settings.option_b | escape }}" type="radio">
              <label for="toggle-off-q{{ forloop.index }}" class="btn"><span>B</span> {{ block.settings.option_b }}</label>
            </div>
          </div>
        {% elsif block.type == 'text_question' %}
          <div class="input-block">
            <div class="label" style="font-family: {{ section.settings.font_family }}; font-size: {{ section.settings.text_font_size }}px; color: {{ section.settings.text_color }};">{{ block.settings.question_label }}</div>
            <div class="input-control">
              <input type="text" class="{% if block.settings.required %}required{% endif %}" autocomplete="off">
            </div>
          </div>
        {% endif %}
      {% endfor %}
    </div>

    <div class="upform-footer">
      <button type="submit" class="btn btn-primary btn-lg" style="background-color: {{ section.settings.button_color }}; color: {{ section.settings.button_text_color }}; font-family: {{ section.settings.font_family }}; font-size: {{ section.settings.button_font_size }}px;">Submit</button>
    </div>
  </form>
</div>

{% javascript %}

  document.querySelectorAll('.upform').forEach(function(form) {
  var container = form.querySelector('.upform-main');

  document.addEventListener('DOMContentLoaded', function() {
    container.querySelector('.input-block').click();
  });

  form.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault();
  });

  container.querySelectorAll('.input-block').forEach(function(block) {
    if (!block.querySelector('input')) {
      block.addEventListener('click', function() {
        rescroll(this);
      });
    }
  });

  container.querySelectorAll('.input-block input').forEach(function(input) {
    input.addEventListener('keypress', function(e) {
      if (e.which === 13) {
        if (this.classList.contains('required') && this.value === '') {
          // Do nothing if required field is empty
        } else {
          moveNext(this);
        }
      }
    });
  });

  container.querySelectorAll('.input-block input[type="radio"]').forEach(function(radio) {
    radio.addEventListener('change', function() {
      moveNext(this);
    });
  });

  window.addEventListener('scroll', function() {
    container.querySelectorAll('.input-block').forEach(function(block) {
      var etop = block.getBoundingClientRect().top;
      var diff = etop - window.scrollY;

      if (diff > 100 && diff < 300) {
        reinitState(block);
      }
    });
  });

  function reinitState(e) {
    container.querySelectorAll('.input-block').forEach(function(block) {
      block.classList.remove('active');
      block.querySelectorAll('input').forEach(function(input) {
        input.blur();
      });
    });
    e.classList.add('active');
    // Uncomment the next line if you want to focus on the input
    // e.querySelector('input').focus();
  }

  function rescroll(e) {
    var offset = e.getBoundingClientRect().top + window.scrollY - 200;
    window.scrollTo({ top: offset, left: 100, behavior: 'smooth' });
  }

  function moveNext(e) {
    var nextBlock = e.closest('.input-block').nextElementSibling;
    if (nextBlock) {
      nextBlock.click();
    }
  }

  function movePrev(e) {
    var prevBlock = e.closest('.input-block').previousElementSibling;
    if (prevBlock) {
      prevBlock.click();
    }
  }
});


  {% endjavascript %}
